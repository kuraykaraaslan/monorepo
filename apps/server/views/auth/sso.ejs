<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SSO Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="w-full max-w-md bg-white rounded-lg shadow-md p-8">
        <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Single Sign-On (SSO)</h2>
        <% if (message) { %>
            <p class="text-red-500 text-center mb-4">
                <%= message %>
            </p>
            <% } %>
                <button onclick="window.location.href='/api/v1/sso/google'"
                    class="w-full bg-red-500 text-white font-semibold py-2 rounded-lg hover:bg-red-600 transition duration-300">Login
                    with Google</button>
                <button onclick="window.location.href='/api/v1/sso/apple'"
                    class="w-full bg-black text-white font-semibold py-2 rounded-lg hover:bg-gray-900 transition duration-300 mt-4">Login
                    with Apple</button>
                <p class="text-center text-gray-600 mt-4">Don't have an account? <a href="/api/v1/auth/register"
                        class="text-blue-500 hover:underline">Sign up</a></p>
                
    </div>
</body>

</html>